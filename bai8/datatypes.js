/* 
    data type :  kiểu dữ liệu
    trong js chia làm 2 kiểu dữ liệu
    1. kiểu dữ liệu nguyên thuỷ hay còn gọi  là kiểu dữ liệu đơn giản 
    hay còn gọi là primitive type (tham trị)
    + number    : số 
    + string    : kiểu chuỗi 
    + boolean   : đúng sai 
    + null      : giá trị rỗng làm việc với object
    + undefined : không có giá trị làm việc với nguyên thuỷ
    + NaN       : not a number
    + Symbol  ES6.
    + BigInt  ES14.
    -kiểm tra kiểu dữ liệu dùng typeof()
    2. kiểu dữ liệu phức tạp hay còn gọi là reference type( tham chiếu)
     + object
     + function
     + array
*/
   /*  let a;
    console.log(a);
    // khai báo viến cấp phát ô nhớ là không có giá  trị
    let b=null;
    // khai báo biến có ô nhớ có giá trị là null
    let result=localStorage.getItem("test");
    console.log(null); */
    /* let c=6;
    let result= "5h" -5 ;
    console.log(Number.isNaN(result)); */
    console.log("");
